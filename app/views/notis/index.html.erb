<% if session[:user_id] == @user.id %>

    <div class="panel panel-default">
      <!-- Default panel contents -->
      <div class="panel-heading"><h3>活动列表</h3>
      </div>
      <div class="panel-body">


        <!-- Table -->
        <table class="table">
          <thead>
          <tr>
            <th>标题</th>
            <th>日期</th>
            <th>发布人</th>

            <th></th>
            <th></th>
            <th></th>
          </tr>
          </thead>

          <tbody>
          <% @notis.each do |noti| %>
              <tr>

                <td><%= noti.title %></td>
                <td><%= noti.date %></td>
                <td><%= noti.user.name %></td>

                <td><%= link_to '编辑', edit_user_noti_path(@user, noti) %></td>
                <td><%= link_to '删除', [@user, noti], method: :delete, data: {confirm: 'Are you sure?'} %></td>
              </tr>
          <% end %>
          </tbody>
        </table>
      </div>
    </div>

    <td><%= link_to raw('<button type="button" class="btn btn-primary">返回</button>'), root_url %></td>

<% else %>
    <%= render "static_page/access_error" %>
<% end %>


